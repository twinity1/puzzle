export async function prepare(context) {
}

export async function setup(context) {
}

// add common prompt for all pieces here
export async function prompt(context) {
    return {
        prompt: ``
    };
}
